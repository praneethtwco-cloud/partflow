16:32:25 - Processing 1 queued operations from offline period...
16:32:25 - Processing 1 queued operations from offline period...
16:32:25 - Failed to sync queued operations: Failed to upload orders: Could not find the 'custom_discount_rate' column of 'orders' in the schema cache
16:32:25 - Error: Failed to sync queued operations: Failed to upload orders: Could not find the 'custom_discount_rate' column of 'orders' in the schema cache



-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.customers (
  customer_id text NOT NULL,
  shop_name text NOT NULL,
  address text,
  phone text,
  city_ref text,
  city text,
  discount_rate numeric DEFAULT 0,
  discount_1 numeric,
  discount_2 numeric,
  secondary_discount_rate numeric DEFAULT 0,
  outstanding_balance numeric DEFAULT 0,
  balance numeric,
  credit_period integer DEFAULT 0,
  credit_limit numeric,
  status USER-DEFINED DEFAULT 'active'::entity_status,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'pending'::sync_status,
  last_updated timestamp with time zone,
  CONSTRAINT customers_pkey PRIMARY KEY (customer_id)
);
CREATE TABLE public.items (
  item_id text NOT NULL,
  item_display_name text NOT NULL,
  item_name text,
  internal_name text,
  item_number text UNIQUE,
  vehicle_model text,
  source_brand text,
  brand_origin text,
  category text,
  unit_value numeric DEFAULT 0,
  current_stock_qty integer DEFAULT 0,
  low_stock_threshold integer DEFAULT 0,
  is_out_of_stock boolean DEFAULT false,
  stock_qty integer,
  low_stock_threshold_csv integer,
  status USER-DEFINED DEFAULT 'active'::entity_status,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'pending'::sync_status,
  last_updated timestamp with time zone,
  CONSTRAINT items_pkey PRIMARY KEY (item_id)
);
CREATE TABLE public.order_lines (
  line_id text NOT NULL,
  order_id text,
  item_id text,
  item_name text,
  quantity integer DEFAULT 0,
  unit_value numeric DEFAULT 0,
  unit_price numeric,
  line_total numeric DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT order_lines_pkey PRIMARY KEY (line_id),
  CONSTRAINT order_lines_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(order_id),
  CONSTRAINT order_lines_item_id_fkey FOREIGN KEY (item_id) REFERENCES public.items(item_id)
);
CREATE TABLE public.orders (
  order_id text NOT NULL,
  customer_id text,
  rep_id text,
  order_date date NOT NULL,
  disc_1_rate numeric,
  disc_1_value numeric,
  disc_2_rate numeric,
  disc_2_value numeric,
  discount_rate numeric DEFAULT 0,
  discount_value numeric DEFAULT 0,
  gross_total numeric DEFAULT 0,
  secondary_discount_rate numeric DEFAULT 0,
  secondary_discount_value numeric DEFAULT 0,
  tax_rate numeric DEFAULT 0,
  tax_value numeric DEFAULT 0,
  net_total numeric DEFAULT 0,
  credit_period integer DEFAULT 0,
  paid_amount numeric DEFAULT 0,
  paid numeric,
  balance_due numeric DEFAULT 0,
  payment_status USER-DEFINED DEFAULT 'unpaid'::payment_status,
  payments jsonb DEFAULT '[]'::jsonb,
  delivery_status USER-DEFINED DEFAULT 'pending'::delivery_status,
  delivery_notes text,
  order_status USER-DEFINED DEFAULT 'draft'::order_status,
  status text,
  invoice_number text,
  approval_status USER-DEFINED DEFAULT 'draft'::approval_status,
  original_invoice_number text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'pending'::sync_status,
  last_updated timestamp with time zone,
  CONSTRAINT orders_pkey PRIMARY KEY (order_id),
  CONSTRAINT orders_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(customer_id)
);
CREATE TABLE public.route_plans (
  id text NOT NULL,
  customer_id text,
  visit_time text,
  note text,
  route_date text,
  created_at timestamp with time zone DEFAULT now(),
  sync_status text DEFAULT 'pending'::text,
  CONSTRAINT route_plans_pkey PRIMARY KEY (id)
);
CREATE TABLE public.settings (
  id text NOT NULL DEFAULT 'main'::text,
  company_name text,
  address text,
  phone text,
  rep_name text,
  invoice_prefix text DEFAULT 'INV'::text,
  starting_invoice_number integer DEFAULT 1,
  footer_note text,
  currency_symbol text DEFAULT '$'::text,
  tax_rate numeric DEFAULT 0,
  auto_sku_enabled boolean DEFAULT true,
  stock_tracking_enabled boolean DEFAULT false,
  category_enabled boolean DEFAULT false,
  show_sku_in_item_cards boolean DEFAULT false,
  logo_base64 text,
  show_advanced_sync_options boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'pending'::sync_status,
  last_updated timestamp with time zone,
  CONSTRAINT settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.stock_adjustments (
  adjustment_id text NOT NULL,
  item_id text,
  adjustment_type USER-DEFINED NOT NULL,
  quantity integer NOT NULL,
  reason text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  sync_status USER-DEFINED DEFAULT 'pending'::sync_status,
  last_updated timestamp with time zone,
  CONSTRAINT stock_adjustments_pkey PRIMARY KEY (adjustment_id),
  CONSTRAINT stock_adjustments_item_id_fkey FOREIGN KEY (item_id) REFERENCES public.items(item_id)
);
CREATE TABLE public.users (
  id text NOT NULL,
  username text NOT NULL UNIQUE,
  full_name text,
  role USER-DEFINED DEFAULT 'rep'::user_role,
  password text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT users_pkey PRIMARY KEY (id)
);
CREATE TABLE public.visits (
  id text NOT NULL,
  customer_id text,
  plan_id text,
  check_in_time timestamp with time zone,
  check_out_time timestamp with time zone,
  check_in_note text,
  check_out_note text,
  status text DEFAULT 'checked_in'::text,
  route_date text,
  created_at timestamp with time zone DEFAULT now(),
  sync_status text DEFAULT 'pending'::text,
  CONSTRAINT visits_pkey PRIMARY KEY (id)
);